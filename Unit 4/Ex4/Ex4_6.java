package com.introduction.unit4.Ex4;

public class Ex4_6 {
    byte b;
    short s;
    int i;
    long l;
    float f;
    double d;
    char c;
    boolean b1, b2;
    
    
    /*
     * b+10L  long 타입
     * (b+i)*l long 타입
     * (b+i)*l+f float 타입
     * s/f + Math.sin(f) float 타입 
     * c == 'b' char 타입
     * l+1.5f float 타입
     * i<10 boolean 타입
     * b1 == (f >= 5.0) boolean 타입 
     * b1 && !b2 boolean 타입
     * 
     */
    
}
