package org.example;

public class E6 {
    /*
    b+10L : long
    (b+i)*l : long
    (b+i)*l+f : float
    s/f + Math.sin(f) : double
    c == 'b' : boolean
    l+1.5f : float
    i<10 : boolean
    b1 == (f >= 5.0) : boolean
    b1 && !b2 : boolean
     */
}
